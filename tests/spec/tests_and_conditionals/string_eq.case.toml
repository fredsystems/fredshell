description = "`[[ \"a\" == \"a\" ]]` succeeds and the `&&` branch runs"
script = """
[[ "abc" == "abc" ]] && echo match
"""
status = "deferred:PLAN_11"
tags = ["tests-and-conditionals", "double-bracket"]
