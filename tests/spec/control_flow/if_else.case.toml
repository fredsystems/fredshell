description = "`if true; then ...; else ...; fi` takes the `then` branch"
script = """
if true; then
    echo yes
else
    echo no
fi
"""
status = "deferred:PLAN_06"
tags = ["control-flow", "if", "posix-overlap"]
